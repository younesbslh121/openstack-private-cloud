# ============================================================
# Heat Template — Environment File
# ============================================================
# Usage: openstack stack create -t web-stack.yaml -e env.yaml my-stack
# ============================================================

parameters:
  image: CirrOS-0.6.2
  flavor: m1.small
  key_name: default-keypair
  external_network: external-net
  instance_count: 2
  app_port: 80
